To run etl.sh: ./etl.sh <server_IP> <server_username> <filename>

All the scripts used are called within etl.sh 

Before ech script is ran it displaying what it's doing, a success message or an unsuccessful message with descriptions
